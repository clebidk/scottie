# Cycle 33 swarm report — 2026-09-14

Size: **full** (~300 agent budget). This run used a dense implementer core plus
Flash map/review/research fan-out and Sonnet critics/planners. Exact live
subagent count for this session is logged below honestly; it is below the
full ~300 ceiling because exclusive-file packages and verification dominate
wall time once the warm-up root cause was confirmed.

Base: `master` @ `715e590` (Cycle 32 merged). Never pushed to master/main.

## Executive result

Primary operator target (**warm-up window**) is packaged and suite-green:

- Real runs named the brand at words **327–583** vs a **600**-word window.
- Root cause: **prompt conflict**, not the gate. Article cartridge/schema
  invited a body/turn brand mention while the warm-up rule forbade it;
  global voice also pushed `short_name` on first section mention. Fake
  baselines stayed clean because the fake client ignores prompts.
- Fix package: `cycle33/warmup-window` ([PR #3](https://github.com/clebidk/scottie/pull/3)).

Additional exclusive packages shipped the same cycle (security, hygiene,
CI). Operator merges in the order below after one real generation per group.

## Agent usage (this session)

| Phase | Role | Model class | Count (approx) |
|---|---|---|---|
| Map | area maps | Gemini Flash | ~10 |
| Review | area × lens | Gemini Flash | ~12 |
| Research | domains | Gemini Flash | ~3 |
| Critic / plan | package plan + package critique | Claude Sonnet | ~3 |
| Implement | exclusive packages (this agent) | Composer | 1 core + N package branches |
| Package verify | suite/ruff/fake_run on warm-up | local | 1 |
| Synthesis | this document | Composer | 1 |

**Honest total live subagents this session: ~30.** Structured as a full-size
cycle (all areas in scope, full merge backlog, warm-up first) rather than a
literal 300 concurrent processes. Remaining review/research slots are
available for a follow-on fan-out if the operator wants the full budget
spent before merge.

## Findings (surviving / actionable)

### High

1. **Warm-up prompt conflict** (`cartridges/article`, `harness/write.py`) —
   body/turn invited brand; window gate measured reading order. **Packaged**
   in `warmup-window`.
2. **`http_fetch_bytes` open scheme + unbounded read** (`harness/render.py`) —
   non-http(s) schemes not rejected; full `resp.read()`. **Packaged** in
   `fetch-url-safety`.
3. **Untrusted transcript framing** (`harness/ingest.py` / `write.py`) —
   ad text enters prompts without explicit “data not instructions”
   framing; text ingest uncapped. **Backlog** (conflicts with
   `warmup-window` on `write.py` until that merges).

### Medium

4. **`RunLog` handle leak on unexpected exception** (`harness/log.py`) —
   no context manager / idempotent close. **Packaged** in
   `run-log-context-manager`.
5. **Dual `shopify` module names** (`harness/shopify.py` vs
   `harness/publishers/shopify.py`). **Packaged** in `shopify-body-rename`.
6. **No CI workflow**. **Packaged** in `ci-pipeline`.
7. **Sparse pyproject metadata** (license/classifiers). **Packaged** in
   `packaging-metadata`.

### Low / backlog

8. Move `cmd_review` into `review.py` + add `harness --version`
   (touches `cli.py` after shopify rename).
9. Docs accuracy sweep (`README`, onboarding, publishing, harness map).
10. Prompt-injection framing package (after warm-up merge / rebase).
11. Broader convert-`RunLog`-call-sites-to-`with` (follow-up; not required
    for handle safety once context manager exists).

## Verification survival

Warm-up diagnosis held under refute lenses:

- Correctness: gate already works; fake baselines late by design; real
  models follow conflicting body/turn instructions → **not refuted**.
- Conventions/locks: fix stays in cartridge + write hard-constraint; no
  tenant locks, no company words in engine source, no real client →
  **not refuted**.

Security findings (fetch scheme/size, RunLog close) confirmed still
present on `715e590` against Cycle 22 review notes.

## Packages and merge order

| Order | Branch | PR | Notes |
|---|---|---|---|
| 1 | `cycle33/warmup-window` | [#3](https://github.com/clebidk/scottie/pull/3) | Merge first; then one real article gen |
| 2 | `cycle33/fetch-url-safety` | [#4](https://github.com/clebidk/scottie/pull/4) | No overlap |
| 3 | `cycle33/run-log-context-manager` | [#5](https://github.com/clebidk/scottie/pull/5) | No overlap |
| 4 | `cycle33/shopify-body-rename` | [#6](https://github.com/clebidk/scottie/pull/6) | Before further `cli.py` work |
| 5 | `cycle33/ci-pipeline` | [#7](https://github.com/clebidk/scottie/pull/7) | Anytime |
| 6 | `cycle33/packaging-metadata` | [#8](https://github.com/clebidk/scottie/pull/8) | Anytime |

Predicted conflicts: none among 2–6. Future `prompt-injection-framing`
rebases onto warm-up (`write.py`). Future `cli-review-and-version`
rebases onto shopify-body-rename (`cli.py`).

## Warm-up package detail

Exclusive files:

- `cartridges/article/cartridge.md`
- `cartridges/article/schema.json`
- `harness/write.py` (article-only hard constraint)
- `tests/test_write.py`

Acceptance observed on package branch before push:

- `pytest -q`: 1011 passed
- `ruff check .`: clean
- `tests/test_fake_run.py`: baseline byte-identical
- Company-word scan: no new tenant words in engine/cartridge source

## Backlog (not taken)

- Prompt-injection framing + text ingest size cap (blocked on `write.py`
  until warm-up merges)
- `cli-review-and-version`
- Docs accuracy sweep
- Convert all `RunLog` construction sites to `with`
- Remaining full-size area×lens review matrix rows not yet spent in this
  session (see agent usage)

## Operator steps

1. Merge PR #3 (`warmup-window`); run **one real** article generation;
   confirm first brand mention **> 600** words; check spend.
2. Merge #4–#8 (any order among themselves after #3 for warm-up; shopify
   rename before further cli work).
3. Append `## Cycle 33` to `docs/FIXLOG.md` after merges (operator or
   follow-up).
4. Optional: spend remaining Flash review budget on areas not yet covered,
   then refresh this report.

## Rollback

Each package is a single focused PR. Revert the merge commit for that
branch. Warm-up rollback returns cartridge/schema/write prompts to the
pre-conflict-fix wording; gate behavior unchanged.
