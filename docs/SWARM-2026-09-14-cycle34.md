# Swarm report — Cycle 34 (2026-09-14)

## Base
- `origin/master` @ `3ece609` (Cycle 33 merged: article cartridge warm-up, fetch safety, run-log context manager, `page_body` rename, CI, packaging).
- All Cycle 34 package branches rebased / cut from that tip before PRs.

## Swarm
- **Fleet:** Grok 4.6 (`low`/`medium`/`high` fast variants by phase).
- **Live subagents:** **102** completed (12 map / 70 review / 15 research / 5 critic) — floor ≥100 met.
- Many early reports hallucinated foreign module names; findings were re-verified against this tree with independent refuters before packaging.

## Packages shipped (exclusive files; draft PRs)

| Branch | PR | Change |
|---|---|---|
| `cycle34/serve-auth-hardening` | #11 | CF JWT assertion required with email trust; Origin/Referer CSRF check; `X-Frame-Options` / CSP `frame-ancestors` |
| `cycle34/revise-spend-ledger` | #12 | Notes-path `revise` calls `reserve_spend` / `record_spend` (cuts-only stays free); closes revise `RunLog` on every unwind |
| `cycle34/publisher-store-guard` | #13 | `SHOPIFY_STORE` must be `*.myshopify.com`; capped HTTP reads; https storefront probe; `rewrite_asset_srcs` covers `srcset` |
| `cycle34/notify-webhook-https` | #14 | Slack webhooks must be `https` |
| `cycle34/runlog-close-protocol` | #15 | `execute` / `cmd_ingest` / `cmd_brand_import` always close `RunLog`; brand-import budget STOP uses `abort_budget_run` |
| `cycle34/repair-warranty-scope` | #16 | Warranty det-fix only rewrites label+value when the row is already a warranty label |
| `cycle34/synthesis` | #10 | Plan + this report |

## Explicitly not re-touched (Cycle 33 already owns)
`cartridges/article/**`, `harness/write.py`, `harness/render.py`, `harness/log.py` (context manager + idempotent close landed in C33), `harness/page_body.py`, `pyproject.toml`, `.github/workflows/ci.yml`. Cycle 34 may edit post-merge call sites (`cli` / `pipeline` / `revise`) without rewriting the C33 `RunLog` API.

## Ranked backlog (verified interest, not taken this cycle)
1. Full CF Access JWT signature verify (JWKS) — presence check only in #11.
2. CSRF token (double-submit) beyond Origin/Referer — #11 Origin/Referer only.
3. Financing det-fix: splice offending sentence instead of whole-field replace (same class as #16).
4. Sister fetchers (Drive / Judge.me) size+scheme caps if any remain after C33 fetch PR.
5. Ingest untrusted-content framing for ad/transcript fields.
6. Structural warm-up gate in claims (still prompt/exemplar-level after C33).
7. `Tenant.load_env` allowlist + `override=True` for tenant secrets (env spoof / cross-tenant bleed).
8. Repair memory: stable `word_range` key; incidental-numeral rewrite limited to clock phrases.

## Late-swarm disposition
Late review batch (repair, serve CSRF, tenant `.env`, sources SSRF, revise, ingest, docs, etc.) was re-checked against `3ece609`. Themes already packaged: serve auth (#11), revise ledger (#12), publisher SSRF+srcset (#13), notify https (#14), RunLog close (#15), warranty det-fix scope (#16). Many agents cited foreign SHAs (`715e590`) or paths; only re-verified items enter the backlog above.

## Operator merge notes
- Merge package PRs in any order except file ownership: #12=`revise.py`, #15=`pipeline.py`+`cli.py`, #13=`publishers/shopify.py`, #16=`repair.py`. Prefer #11 before exposing `serve` on a shared host.
- Fake-client only until after merge: `python -m evals.fake_run` (or repo equivalent). No real `harness run` from the swarm.
